name: myflutterapp
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.4.0
  cupertino_icons: ^1.0.8
  http: ^1.3.0          # Updated to latest version
  uuid: ^4.5.1          # Updated to latest version
  file: ^7.0.1          # Latest version
  material_color_utilities: ^0.11.1  # Downgraded to match flutter_test dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
